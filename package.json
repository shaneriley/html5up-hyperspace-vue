{
  "name": "html5up-hyperspace-vue",
  "description": "HTML5Up's Hyperspace template rewritten as Vue components",
  "version": "1.0.0",
  "main": "src/app.js",
  "license": "CC BY 3.0",
  "scripts": {
    "start": "concurrently 'http-server' 'node-sass-chokidar --output ./assets/css --recursive --watch --source-comments ./assets/sass'"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "http-server": "^0.12.3",
    "node-sass-chokidar": "^1.4.0"
  }
}
